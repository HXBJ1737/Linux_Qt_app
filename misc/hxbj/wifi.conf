

ctrl_interface=/var/run/wpa_supplicant
ap_scan=1
update_config=1

network={
        ssid="iQOO_Neo10."   # Wi-Fi名字
        psk="death001"  # Wi-Fi密码
        key_mgmt=WPA-PSK  # 加密配置，不加密则改为：key_mgmt=NONE
}
